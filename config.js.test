var path = require('path')

var config = {
  secret: 'verysecret',
  testMode: true,
  baseUrl: 'http://localhost/',
  authenticators: {anonymous: {}}, // Using defaults.
  database: 'sqlite://' + path.join(__dirname, 'build', 'comments-test.db'),
}

module.exports = config
