var path = require('path')

var config = {
  secret: 'verysecret',
  testMode: true,
  server: {
    domain: 'localhost'
  },
  connectors: {},
  database: 'sqlite://' +path.join(__dirname, 'comments-test.db'),
}

module.exports = config
